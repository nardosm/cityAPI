// Here is where we're handing a ticket-request system.

const express = require(`express`);
const router = express.Router();

/* GET home page. */
router.get(`/`, (req, res, next) => {
  // Nothing here currently.
});

module.exports = router;